{
  "name": "maintenance-app-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2",
    "date-fns": "^2.30.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "lint": "node scripts/verify-react-scripts.js && eslint \"src/**/*.{js,jsx}\"",
    "prestart": "node scripts/verify-react-scripts.js",
    "start": "react-scripts start",
    "prebuild": "node scripts/verify-react-scripts.js",
    "build": "react-scripts build",
    "pretest": "node scripts/verify-react-scripts.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
